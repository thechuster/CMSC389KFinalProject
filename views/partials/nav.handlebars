<div class="nav">
    <h1>Album Reviews</h1>
    {{#if tags}}
    <div class="tags">
    	{{#each tags}}
	        <a href="/tag/{{this}}" class="tag">{{this}}</a>
        {{/each}}

        <a href="/members" class="tag">Group Members</a>
    </div>
    {{/if}}
</div>
<br><hr>